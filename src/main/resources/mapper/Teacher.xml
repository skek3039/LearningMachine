<?xml version="1.0" encoding="UTF-8" ?> <!-- 맵퍼파일 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="Teacher">

  	<select id="lectureList" resultType="LectureDTO">
  		SELECT la_no, t_id, l_name, l_category, l_category, l_curriculum, l_date, l_recognize
		FROM lecture_apply
		WHERE l_recognize = 0 
  	</select>
  
  </mapper>