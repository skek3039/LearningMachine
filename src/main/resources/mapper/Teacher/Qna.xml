<?xml version="1.0" encoding="UTF-8" ?> <!-- 맵퍼파일 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="QnA">

	<select id="qnaList" resultType="lectureDTO" parameterType="lectureDTO">
		SELECT * 
		,(SELECT u_nickname FROM user WHERE u_id = lecture_qna.u_id) AS u_id 
		FROM lecture_qna 
		WHERE lqa_confirm = 0
	</select>

  </mapper>