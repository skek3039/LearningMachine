<?xml version="1.0" encoding="UTF-8" ?> <!-- 맵퍼파일 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="Login">

  	<select id="login"  parameterType="userDTO" resultType="userDTO">
  		select * from user where u_id=#{u_id} and u_pw = #{u_pw}
  	</select>
  
  <insert id="join" parameterType="userDTO">
		insert into  user (u_id, u_pw, u_email, u_name, u_nickname, u_gender, u_birth)
		values (#{u_id},#{u_pw},#{u_email},#{u_name},#{u_nickname},#{u_gender},#{u_birth})
  </insert>
  
  <select id="checkID" parameterType="String" resultType="integer">
  	select count(*) as count FROM user where u_id=#{u_id}
  </select>
  
  <update id="resetPw" parameterType="userDTO" >
  	 update user set u_pw=#{u_pw} where u_id=#{u_id} AND u_email=#{u_email}
  </update>
  
  <select id="Check" parameterType="userDTO" resultType="userDTO">
  	select * FROM user where u_id=#{u_id} AND u_email=#{u_email}
  </select>
  
  
  </mapper>